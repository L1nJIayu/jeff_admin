const PREFIX = '__JEFF_ADMIN__'

export const AUTH = `${PREFIX}AUTH`
export const PERMISSION = `${PREFIX}PERMISSION`
export const TAB_LIST = `${PREFIX}TAB_LIST`
export const TAB_ACTIVE_KEY = `${PREFIX}TAB_ACTIVE_KEY`